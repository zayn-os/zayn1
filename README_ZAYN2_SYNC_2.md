# دليل المزامنة السحابية (Cloud Sync) - الجزء الثاني: النسخ الاحتياطي والاستعادة 📦

هذا الجزء يشرح كيفية التعامل مع ملفات النسخ الاحتياطي (Backup Files) يدوياً وتلقائياً.

## 1. النسخ التلقائي (Auto-Backup)
بمجرد تسجيل الدخول بحساب Google:
- يقوم النظام برفع نسخة كاملة من قاعدة بياناتك (JSON Dump) إلى Firestore كل 24 ساعة.
- يتم حفظ آخر 7 نسخ احتياطية (لآخر أسبوع).

## 2. الاستعادة اليدوية (Manual Restore)
إذا حدث خطأ كارثي (مثلاً حذفت غارة بالخطأ):
1.  اذهب إلى **Settings** > **Data Management**.
2.  اضغط **"Restore from Cloud"**.
3.  اختر تاريخ النسخة التي تريد العودة إليها (مثلاً: "Yesterday 10:00 PM").
4.  سيقوم النظام باستبدال البيانات الحالية بالنسخة القديمة.

## 3. التصدير والاستيراد (Export/Import)
للمحترفين الذين يريدون نقل البيانات يدوياً:
- **Export JSON:** يحمل ملفاً نصياً بكل بياناتك.
- **Import JSON:** يقرأ ملفاً ويستبدل البيانات الحالية.
- **Neural Injection:** يدمج بيانات جديدة (مثل غارة) دون مسح القديم.

---
*تم إعداد هذا الدليل بواسطة نظام Zayn2 للمزامنة.*
