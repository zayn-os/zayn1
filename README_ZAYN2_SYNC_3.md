# دليل المزامنة السحابية (Cloud Sync) - الجزء الثالث: الأمان والخصوصية 🔒

هذا الدليل يشرح كيف نحمي بياناتك الحساسة.

## 1. التشفير (Encryption)
- جميع البيانات المرسلة بين هاتفك وسيرفرات Google مشفرة بتقنية SSL/TLS.
- لا أحد (حتى مطور التطبيق) يستطيع قراءة مهامك الخاصة إلا أنت.

## 2. قواعد البيانات (Firestore Rules)
نستخدم قواعد صارمة في Firebase:
```javascript
match /users/{userId} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}
```
- **المعنى:** "لا يسمح لأحد بقراءة أو كتابة بيانات المستخدم {userId} إلا صاحب الحساب نفسه".

## 3. تسجيل الخروج (Logout)
- عند تسجيل الخروج، يتم مسح البيانات المحلية الحساسة من الجهاز (للحماية إذا كان الجهاز مشتركاً).
- البيانات السحابية تبقى آمنة وتنتظر عودتك.

---
*تم إكمال سلسلة أدلة المزامنة بنجاح بواسطة نظام Zayn2.*
