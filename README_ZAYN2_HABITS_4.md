# دليل العادات (Habits) - الجزء الرابع: الحقن والتعديل الذكي 💉

هذا الدليل مخصص للمحترفين: كيفية إنشاء وتعديل العادات باستخدام كود JSON عبر الـ Neural Injection.

## 1. الهيكل الأساسي (JSON Structure)
لإنشاء عادة جديدة، استخدم هذا القالب:

```json
{
  "habits": [
    {
      "title": "Morning Run",
      "difficulty": "hard",
      "stat": "STR",
      "type": "daily",
      "dailyTarget": 1,
      "skillId": "sk_running_101",
      "description": "5km run at zone 2 heart rate."
    }
  ]
}
```

## 2. الحقول المتقدمة (Advanced Fields)

### أ. التكرارات (Repetitions)
لجعل العادة تتطلب 5 مرات يومياً:
```json
"dailyTarget": 5
```

### ب. أيام محددة (Specific Days)
لجعلها السبت (6) والثلاثاء (2):
```json
"type": "specific_days",
"specificDays": [6, 2]
```

### ج. المهام الفرعية (Subtasks)
```json
"subtasks": [
  { "title": "Put on shoes" },
  { "title": "Warm up" },
  { "title": "Run" }
]
```

## 3. التعديل الذكي (Smart Update)
لتعديل عادة موجودة بدلاً من إنشاء واحدة جديدة، **يجب** استخدام الـ `id` الخاص بها.

1.  احصل على الـ `id` من التصدير (مثلاً `h_12345`).
2.  احقن كود JSON يحتوي على الـ `id` والتغييرات فقط.

**مثال: تغيير صعوبة عادة موجودة إلى Hard:**
```json
{
  "habits": [
    {
      "id": "h_12345",
      "difficulty": "hard"
    }
  ]
}
```
*سيقوم النظام بالبحث عن العادة رقم `h_12345` وتحديث صعوبتها فقط، مع الحفاظ على سلسلتها وتاريخها.*

## 4. الحذف والأرشفة
حالياً، الحذف والأرشفة يتم يدوياً من واجهة المستخدم لضمان عدم فقدان البيانات بالخطأ عبر الحقن.

---
*تم إكمال سلسلة أدلة العادات بنجاح بواسطة نظام Zayn2.*
